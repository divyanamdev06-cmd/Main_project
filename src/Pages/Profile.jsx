import UserProfile from "../components/userDashboard/UserProfile.jsx";

/** Admin profile — same API-backed editor as seekers/recruiters (account-focused). */
export default function Profile() {
  return <UserProfile />;
}
